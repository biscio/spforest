# load(file = "data/Xtesttess.RData") # How to make it work when running "test_all()"??
# 
# expect_silent(
#   tessforest <- intensity.forest(
#     X = Xtesttess,
#     lambda = NULL,
#     Ntrees = 2,
#     at = NULL,
#     mc.cores = 2,
#     test.connected = TRUE
#   )
# )
